version: '3.8'

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: mcp_chat
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./server/db/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
    restart: unless-stopped

  app:
    build:
      context: .
      args:
        VITE_API_URL: ${APP_URL:-http://localhost:4000}
        VITE_GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
    environment:
      PORT: 4000
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_NAME: mcp_chat
      JWT_SECRET: ${JWT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      ALLOWED_ORIGINS: ${APP_URL:-http://localhost:5173}
      MCP_CONNECT_LATEST: ${MCP_CONNECT_LATEST:-1.3.0}
      SYSTEMATICS_ISSUER_URL: ${SYSTEMATICS_ISSUER_URL:-}
      SYSTEMATICS_CLIENT_ID: ${SYSTEMATICS_CLIENT_ID:-}
      SYSTEMATICS_CLIENT_SECRET: ${SYSTEMATICS_CLIENT_SECRET:-}
      SYSTEMATICS_REDIRECT_URI: ${SYSTEMATICS_REDIRECT_URI:-}
    ports:
      - "4000:4000"
    depends_on:
      - db
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    depends_on:
      - app
    restart: unless-stopped

  certbot:
    image: certbot/certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

volumes:
  pgdata:
