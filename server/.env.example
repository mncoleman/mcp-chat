PORT=4000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=mcp_chat
JWT_SECRET=change_me_to_a_random_string
GOOGLE_CLIENT_ID=your_google_client_id

# Sign in with Systematics (optional). Leave blank to disable the button.
# Register a resource at https://app.dovito.com admin -> clients -> [business] -> Resources.
# The redirect URI must exactly match what is registered there.
SYSTEMATICS_ISSUER_URL=https://app.dovito.com
SYSTEMATICS_CLIENT_ID=
SYSTEMATICS_CLIENT_SECRET=
SYSTEMATICS_REDIRECT_URI=http://localhost:4000/api/auth/systematics/callback
